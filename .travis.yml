git:
  submodules: false
  
branches:
  only:
  - master
  
env:
  global:
    - PRODUCT=conversion
    - ROOT_DIR=common
    - SRC_DIR=common/public
    - DEST_DIR=/var/www/products.groupdocs.com/html

before_install:
- openssl aes-256-cbc -K $encrypted_dfdcfd5172af_key -iv $encrypted_dfdcfd5172af_iv
  -in deploy_key.enc -out ./deploy_key -d
- sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
- git submodule update --init --recursive
- git submodule foreach git pull origin master
- eval "$(ssh-agent -s)"
- chmod 600 ./deploy_key
- echo -e "Host $HOST\n\tStrictHostKeyChecking no\n" >> ~/.ssh/config
- ssh-add ./deploy_key

install:
- curl -LO https://github.com/gohugoio/hugo/releases/download/v0.67.1/hugo_extended_0.67.1_Linux-64bit.deb
- sudo dpkg -i hugo_extended_0.67.1_Linux-64bit.deb

script:
- cp -r english $ROOT_DIR/content/
- hugo --source $ROOT_DIR --minify
- zip $PRODUCT.zip $SRC_DIR/$PRODUCT

after_success:
- ls -l
#- scp -i ./deploy_key -r $SRC_DIR/$PRODUCT/index.html $USER@$HOST:$DEST_DIR/$PRODUCT
#- travis_wait 60 scp -i ./deploy_key -r $SRC_DIR/$PRODUCT/java $USER@$HOST:$DEST_DIR/$PRODUCT
#- travis_wait 60 scp -i ./deploy_key -r $SRC_DIR/$PRODUCT/net $USER@$HOST:$DEST_DIR/$PRODUCT
#- scp -i ./deploy_key -r $SRC_DIR/sitemap.xml $USER@$HOST:$DEST_DIR/sitemaps/$PRODUCT.xml
